name: just-echo
on:
    schedule:
      - cron: '*/5 * * * *'

jobs:
    just-echo:
        runs-on: ubuntu-latest
        steps:
          - name: Echo
            run: echo "Hello world!" && false
          - name: Echo final
            run: echo "World finalized"
            if: ${{ !inputs.skip && always() }}
